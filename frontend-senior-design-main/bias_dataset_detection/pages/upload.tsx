import React, { useState } from "react";
import Head from "next/head";
import { useRouter } from "next/router";
import NavBar from "../components/NavBar";
import SubmitData from "../components/SubmitData";
import Metrics from "../components/Metrics";
import Graphs from "../components/Graphs";

function Upload() {
    const [currentComponentIndex, setCurrentComponentIndex] =
        useState<number>(0);

    const arrayComponent = [
        <SubmitData
            setCurrentComponentIndex={setCurrentComponentIndex}
            key={0}
        />,
        <Metrics key={1} />,
        <Graphs key={2} />,
    ];

    const router = useRouter();

    return (
        <div className="bg-[#C3B1E1] min-h-screen">
            <Head>
                <title>Bias Detection in University Data</title>
                <meta
                    name="description"
                    content="Generated by create next app"
                />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <NavBar
                setCurrentComponentIndex={setCurrentComponentIndex}
                currentComponentIndex={currentComponentIndex}
            />
            {arrayComponent[currentComponentIndex]}
        </div>
    );
}

export default Upload;
